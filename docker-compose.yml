services:
  dashboard:
    build: .
    container_name: platform_dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./src:/app/src  # Hot-Reload: Cambios en código se reflejan al instante
    restart: always
